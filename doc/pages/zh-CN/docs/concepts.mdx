# 概念

在使用依赖注入模式之前，需要先掌握以下概念：

- **标识符**，标识符将一个依赖项与其他的依赖项区分开来，它是依赖项的身份标识。
- **依赖项**，依赖项是标识符所对应的具体实现。
- **依赖关系**，依赖关系是指某个依赖项需要依赖何种标识符。
- **绑定**，绑定将一个标识符映射到一个依赖项。
- **注入器**，注入器持有一组绑定，并且暴露方法让其他代码通过标识符得到一个依赖项。

依赖注入的基本概念就只有这些！接下来几节，我们会介绍在 redi 中如何创建标识符、依赖项、绑定和注入器。
